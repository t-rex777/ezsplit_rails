<%= form_with(model: @group_membership) do |form| %>
  <% if @group_membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@group_membership.errors.count, "error") %> prohibited this group membership from being saved:</h2>

      <ul>
        <% @group_membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, "User Id", style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :group_id, "Group Id", style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
